# Docker Compose 用（给 PostgreSQL 容器）
# 复制为 .env 后按需修改；不提交 .env 到 Git

POSTGRES_USER=sports
POSTGRES_PASSWORD=sports_secret
POSTGRES_DB=sports_analytics
# 主机端口，默认 5433 避免与本机 Postgres(5432) 冲突；若 5432 空闲可改为 5432
POSTGRES_PORT=5433
